# 知识库参考与复用指南

这个仓库是森林的个人知识库，同时提供一套可借鉴的 Obsidian 结构。任何人可以复制方法，但必须建立自己的身份、目标、项目和能力证据。

## 第一次使用
1. 克隆仓库：`git clone https://github.com/zhaosenlin12-creator/my_edu.git`
2. 在 Obsidian 选择“打开本地库”，指向克隆后的目录。
3. 先读 `README.md`、`10_Profile` 和 `00_Home`。
4. 如果要参考这套结构，复制模板并重写自己的“三问”，不要直接沿用森林的个人结论。
5. 新增项目、课程或工具时，保留来源、状态和下一步。

## 日常记录
- 项目放 `20_Projects`，课程放 `30_Teaching`，内容放 `40_Content`
- AI 和开发工具放 `50_AI`，外部资料放 `70_Sources`
- 新笔记从 `00_Home/_templates` 复制
- 每张知识卡至少写来源、用途、当前状态、下一步和关联

## Git 协作
1. 开始前 `git pull --rebase`。
2. 一次提交只处理一个主题。
3. 提交前看 `git diff`，不得包含密码、Token、合同和学生隐私。
4. 提交信息示例：`docs: add Python lesson review`。
5. 有冲突先理解双方修改，不直接覆盖。

## AI 与工程规则
- AI 可以整理、检索和实现，但结论必须回到原始资料或可运行结果验证
- 不把“AI 说过”当证据
- Codex 负责协作和修改，IntelliJ 或项目工具链负责编译、运行、测试和调试
- 有复用价值的对话要提炼为知识卡，不能只留在聊天记录里

## 每周验收
- 这周做出了什么可见作品？
- 哪个知识点已经能讲清楚？
- 哪个错误最值得留下？
- 下周最重要的一个动作是什么？

## 公开边界
自己的理解、案例和代码可以按授权分享；外部整站镜像、付费资料、个人隐私和机构敏感数据不提交到公开仓库。
