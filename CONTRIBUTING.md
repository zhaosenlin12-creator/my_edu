# 四位徒弟协作指南

这个仓库首先是森林的个人知识库，也是一套可复制的学习骨架。每位徒弟应建立自己的 Vault，不要把四个人的私人笔记全部塞进同一个仓库。

## 第一次使用
1. 克隆仓库：`git clone https://github.com/zhaosenlin12-creator/my_edu.git`
2. 在 Obsidian 选择“打开本地库”，指向克隆后的目录。
3. 先读 `10_Profile/我是谁.md`、`我在哪.md`、`我要去哪.md`。
4. 复制三页为自己的版本，只写有证据的事实。
5. 打开 `00_Home/本周最重要的事.md`，填写本周三个动作。

## 日常记录
- 项目放 `20_Projects`，课程放 `30_Teaching`，内容放 `40_Content`
- AI 工具和学习路线放 `50_AI`，外部资料放 `70_Sources`
- 新笔记从 `00_Home/_templates` 复制
- 标题说人话，properties 保持统一
- 每张知识卡至少写来源、用途、下一步和关联

## Git 协作
1. 开始前 `git pull --rebase`。
2. 一次提交只处理一个主题。
3. 提交前看 `git diff`，不得包含密码、Token、合同和学生隐私。
4. 提交信息示例：`docs: add Python lesson review`。
5. 有冲突先理解双方修改，不直接覆盖。

## AI 使用规则
- AI 可以整理、检索和实现，但结论必须回到原始资料或可运行结果验证
- 不把“AI 说过”当证据
- 每次让 AI 改代码后都看 diff，并运行对应测试
- 有复用价值的对话要提炼为知识卡，不能只留在聊天记录里

## 每周验收
- 我这周做出了什么可见作品？
- 我能否把其中一个知识点讲给别人？
- 哪个错误最值得留下？
- 下周最重要的一件事是什么？

## 公开边界
自己的理解、案例和代码可以按授权分享；外部整站镜像、付费资料、个人隐私和机构敏感数据不提交到公开仓库。
